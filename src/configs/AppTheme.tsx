import createTheme from "@mui/material/styles/createTheme";

const theme = createTheme({
  palette: {
    primary: {
      main: "#ffc107",
      contrastText: "#ffffff",
    },
    secondary: {
      main: "#fafafa",
    },
  },
});

export default theme;
